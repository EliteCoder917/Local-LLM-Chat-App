Place your application icon here as `icon.ico` (multi-resolution Windows icon).
electron-builder reads `resources/icon.ico` per electron-builder.yml.
A 256x256 PNG converted to .ico is fine; no icon → builder uses a default.
