fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
pydantic>=2.6
httpx>=0.27
# Default engine — loads .gguf files directly. Heavy install (compiles C++).
# Comment out if you only plan to use Ollama.
llama-cpp-python>=0.2.70
